% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_plastics.R
\name{clean_plastics}
\alias{clean_plastics}
\title{Clean and Wrangle the Plastics Dataset}
\usage{
clean_plastics(plastics)
}
\arguments{
\item{plastics}{A data frame containing the raw Break Free From Plastic
dataset, as returned by \code{load_data()}. Expected columns: \code{country},
\code{year}, \code{parent_company}, \code{grand_total}, \code{num_events}, \code{volunteers}.}
}
\value{
A tibble with one row per country-year, containing columns
\code{country}, \code{year}, \code{total_plastic}, \code{num_events}, and \code{volunteers}.
}
\description{
Filters out summary rows, removes countries with missing or zero total
plastic counts, standardizes country name casing, and aggregates to one
row per country per year. Uses \code{data.table} internally for fast aggregation.
}
\examples{
\dontrun{
  raw <- load_data()
  cleaned <- clean_plastics(raw)
}
}
